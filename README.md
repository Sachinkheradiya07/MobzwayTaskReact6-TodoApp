# MobzwayTaskReact6-TodoApp
Todo app built with React and Firebase featuring user authentication, secure login/logout, and task management. Users can create task lists with tasks categorized by priority (low, medium, high). Supports drag-and-drop functionality for easy task reorganization. Data stored in Firebase Firestore for persistence.
